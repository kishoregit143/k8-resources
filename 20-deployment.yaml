apiversion: app/v1
kind: Deployment
metadata:
 name: nginx
 #this labels are replecaset labels
 labels:
  purpose: dp-demo
  project: roboshop
spec:
 replicas: 3
 selector:
 # this lablels used to  select the pods
  matchlabels:
   purpose: dp-demo
   project: roboshop
   #pod defention
  templects:
    metadata:
      labels:
      purpose: dp-demo
      project: roboshop
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
          - containerport: 80