kind: pod
apiversion: v1
metadata:
  name: env-pod
spec:
  containers:
   - name: nginx
     image: nginx
     env:
     - name: course
       value: kubernetes
     - name: author
       value: kishore  

