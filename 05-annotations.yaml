apiversion: v1
kind: pod
metadata:
  name: annotations
annotations:
  jenkins-url: "http://jenkins.roboshop.internal"
  imageregistiry: "docker.io/roboshop"  

spec:
  containers:
   - name: nginx
     image: nginx